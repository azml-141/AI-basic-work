1. 집값을 예측하는 것에서, 집의 나이를 추측하는 것으로 코딩을
바꾸어 보세요. 어떤 문제가 난이도가 높은가요?

집값 예측 MSE = 0.12

답:

데이터를 입력과 출력으로 분리

print('\n분리 전 데이터 모양: ', Z_data.shape)
X_data = Z_data.drop('AGE', axis=1)
Y_data = Z_data['AGE']

집의 나이 예측 MSE = 0.19

 MSE와 산포도를 보았을 때, 손실률이 더 높고 분산이 안좋은 집의 나이를 추측하는 것이 난이도가 더 높은 것 같다.